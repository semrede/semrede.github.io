[Unit]
Description=Count the SemRede visits every hour

[Timer]
OnCalendar=hourly
RandomizedDelaySec=5m
# Catch up after the machine was asleep or switched off.
Persistent=true

[Install]
WantedBy=timers.target
